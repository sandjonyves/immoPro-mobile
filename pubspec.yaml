name: immopro
description: ImmoPro — immobilier Cameroun (Clean Architecture)
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_bloc: ^8.1.5
  equatable: ^2.0.5
  get_it: ^7.6.7
  go_router: ^13.2.0

  flutter_map: ^6.1.0
  flutter_map_marker_cluster: ^1.3.0
  latlong2: ^0.9.0

  geolocator: ^11.0.0
  permission_handler: ^11.3.1

  url_launcher: ^6.3.0

  fl_chart: ^0.68.0

  image_picker: ^1.1.1
  cached_network_image: ^3.3.1

  shared_preferences: ^2.2.3

  intl: ^0.20.2

  uuid: ^4.3.3

  google_fonts: ^6.2.1

  reactive_forms: ^17.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
